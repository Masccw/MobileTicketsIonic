import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.mobileticketsIonic',
  appName: 'MobileTicketsIonic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
